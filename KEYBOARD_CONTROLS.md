# 键盘控制说明

## 智能体控制（Agent Control）

使用 `--keyboard` 参数启动键盘控制模式后，可以使用以下按键控制智能体：

### 基本移动控制（适用于 action_dim >= 3）

- **数字键 2**：向下移动（-y 方向）
- **数字键 4**：向左移动（-x 方向）
- **数字键 6**：向右移动（+x 方向）
- **数字键 8**：向上移动（+y 方向）

### 旋转控制（适用于 action_dim >= 6）

- **z 键**：逆时针旋转（-wz）
- **x 键**：顺时针旋转（+wz）

### 退出

- **q 键**：退出键盘控制模式

---

## 视角控制（Camera Control）

以下按键用于控制渲染窗口中的视角，**与智能体控制独立**：

### 视角移动

- **↑ (UP 箭头键)**：向前移动视角
- **↓ (DOWN 箭头键)**：向后移动视角
- **u 键**：向上移动视角（+y 方向）
- **i 键**：向下移动视角（-y 方向）

### 视角旋转

- **鼠标右键 (RMB) + 拖拽**：旋转视角

---

## 注意事项

1. **按键冲突**：数字键 2/4/6/8 和 u/i 键同时用于智能体控制和视角控制，但它们是独立工作的。如果同时按下，两者都会生效。

2. **控制模式**：根据环境的 `action_dim`，系统会自动选择合适的控制模式：
   - `action_dim >= 6`：支持 vxy 移动和 wz 旋转（使用 2/4/6/8 和 z/x）
   - `action_dim >= 3`：仅支持 vxy 移动（使用 2/4/6/8）
   - `action_dim < 3`：仅支持 wz 旋转（使用 z/x）

3. **环境特定策略**：某些环境可能有自定义的键盘策略（通过 `demo_policy()` 方法），系统会自动检测并使用。

---

## 使用示例

```bash
# 使用配置文件启动键盘控制
python fluidlab/run.py --cfg_file configs/exp_pouring.yaml --keyboard

# 或直接指定环境名称
python fluidlab/run.py --env_name Pouring-v0 --keyboard
```

---

## 控制映射表

| 按键 | 功能 | 说明 |
|------|------|------|
| 2 | 智能体向下移动 | -y 方向 |
| 4 | 智能体向左移动 | -x 方向 |
| 6 | 智能体向右移动 | +x 方向 |
| 8 | 智能体向上移动 | +y 方向 |
| z | 智能体逆时针旋转 | -wz（仅 action_dim >= 6） |
| x | 智能体顺时针旋转 | +wz（仅 action_dim >= 6） |
| q | 退出程序 | 退出键盘控制模式 |
| ↑ | 视角向前移动 | 相机向前 |
| ↓ | 视角向后移动 | 相机向后 |
| u | 视角向上移动 | 相机 +y 方向 |
| i | 视角向下移动 | 相机 -y 方向 |
| 鼠标右键 + 拖拽 | 旋转视角 | 相机旋转 |


